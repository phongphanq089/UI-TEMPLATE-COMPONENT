{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "builds": [
    {
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --force",
        "buildCommand": "npm run build"
      }
    }
  ]
}
